# 주간업무보고 (Weekly Report)

파트원용 주간 업무보고 프로그램. 사내 폐쇄망에서 fat jar + `.bat`으로 실행되는 로컬 Spring Boot 앱.
데이터 교환은 `.md` 파일(사람이 읽는 마크다운 본문 자체가 파싱 가능한 구조)로만 이루어짐.
스키마/설계 원본 문서: `docs/project-handoff-summary.md`(배경·목표·조직구조·데이터모델·화면구성 전체 설계 요약), `docs/weekly-report-md-schema.md`(`.md` 내보내기 포맷 상세) — 파트원 화면만 확정, 파트장/팀장 화면은 범위 밖.

## 하네스: 주간업무보고 개발 자동화

**목표:** 기획/기능/버그 작업을 design·backend·frontend·qa·docs-sync 전문 에이전트로 조율해, 이 프로젝트 고유의 관례(제출=비잠금 정책, htmx outerHTML/none 배선, JPA equals/hashCode 함정, 확정 디자인 언어 등)를 매번 재설명하지 않고도 일관되게 지키며 작업한다.

**트리거:** 이 저장소에서 기능 추가·버그 수정·엔티티/서비스/컨트롤러/템플릿/htmx/CSS/테스트/문서 변경, UI/UX 기획·화면 설계·목업 작업을 요청받으면 `weekly-report-orchestrator` 스킬을 사용하라. 단순 코드 설명·질문은 스킬 없이 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-05 | 초기 구성 (backend/frontend/qa/docs-sync 에이전트 + 오케스트레이터) | 전체 | 사용자 요청("이 프로젝트에 맞는 하네스를 구축해줘") |
| 2026-08-05 | md 내보내기 JSON 블록 제거 작업(첫 실전 실행)에서 드러난 환경 제약 3가지(TeamCreate 부재, 커스텀 subagent_type 즉시 미인식, 서브에이전트 TaskGet/TaskUpdate 부재) 및 우회 절차 반영 | skills/weekly-report-orchestrator | 실제 실행 중 반복 시행착오를 겪어 발견 — 다음 실행부터 재발 방지 |
| 2026-08-05 | 스킬/에이전트 문서 내 "JSON 블록 제거 미착수" 서술을 완료 반영으로 갱신 | skills/spring-backend-dev, skills/weekly-report-docs-sync, agents/backend | md 내보내기 작업 완료에 따른 문서 드리프트 방지(docs-sync 에이전트가 자동 반영) |
| 2026-08-05 | design 에이전트 추가 (UI/UX 기획, 목업 갱신, Artifact 발행+사용자 승인 게이트) 및 오케스트레이터에 design→backend/frontend→qa→docs-sync 순서 반영 | agents/design, skills/weekly-report-design, skills/weekly-report-orchestrator | 사용자 요청("디자인 ui/ux 기획하는 에이전트가 추가로 필요해") |
| 2026-08-15 | 대시보드 재도입(3탭) + 일일 기록(DailyNote) 구현 완료를 문서에 반영 — 화면 구조를 2탭→3탭으로, "다음에 할 일"에서 대시보드 도입·`Project.active` 집계 항목 제거, 기록 검색(`q`) 미결 항목 신설 | CLAUDE.md, docs/2026-08-06-daily-notes-design-worklog.md, docs/2026-08-06-dashboard-redesign-planning.md, docs/2026-08-06-role-roadmap.md | 2026-08-06 기획 라운드가 backend→frontend→qa까지 실제 구현으로 이어져(`_workspace/20260814_234652_dashboard-and-daily-notes-implementation/`) 문서가 코드보다 뒤처짐 |
| 2026-08-15 | "한 일 기록" 본문 검색 구현 완료 반영 — "다음에 할 일"에서 검색(`q`) 미결 항목 제거, 화면 구조에 검색 범위·"검색 중 추가 불가" 정책 추가, htmx 배선 규약에 `hx-select`와 `recordQuery`/`recordQueryRaw` 계약 등재 | CLAUDE.md, docs/2026-08-06-daily-notes-design-worklog.md | backend→frontend→qa 라운드 완료(`_workspace/20260815_052706_history-records-search/`)로 남아 있던 미결 항목이 해소됨 |
| 2026-08-16 | 대시보드 벤토 레이아웃 확정 + 할 일 리스트(`TodoItem`) 구현 완료 반영 — "현재 화면 구조"의 대시보드 서술을 벤토(10행)로 교체, 할 일 리스트 절 신설, htmx 배선 규약에 `#todoCard` 계약과 settle 함정 등재, "다음에 할 일"의 전망형 미착수 항목 해소 표시 | CLAUDE.md, docs/2026-08-06-role-roadmap.md, docs/2026-08-15-todo-feature-review.md | design 3라운드(시안 20종 탐색 후 벤토 확정)→backend→frontend→qa 완료(`_workspace/20260815_164400_bento-todo-implementation/`). qa가 CLAUDE.md에 `TodoItem` 서술이 전혀 없고 대시보드 구조가 개편 전 상태라고 지적 |

## 기술 스택 & 실행

- Java 17 + Spring Boot 3 + Gradle (`./gradlew bootRun`, `./gradlew test`, `./gradlew bootJar`)
- Thymeleaf 서버사이드 렌더링, H2 파일 DB(`~/.weekly-report/data/db`)
- 이 WSL 환경엔 Java/Gradle이 시스템에 없었어서 Rocky Linux에 `dnf`로 Java 17을 설치하고 Gradle 9.4.1(Windows IntelliJ 캐시에서 복사)로 wrapper를 생성해둠 — `./gradlew`만 쓰면 재현 가능
- 배포: `run.bat`이 `javaw -jar weekly-report.jar`로 콘솔 없이 실행 후 기본 브라우저 자동 오픈

## 개발 환경 (WSL)

- 이 프로젝트는 WSL 배포판 **`Rocky9`** 안, `/home/sanghwaj/project/report`에 있음.
- Windows에서 IntelliJ로 열기: `File > Open` → `\\wsl.localhost\Rocky9\home\sanghwaj\project\report` 입력 → "WSL로 열기" 선택(네트워크 폴더로 그냥 열면 느리고 일부 기능 제한). JDK를 못 찾으면 `\\wsl.localhost\Rocky9\usr\lib\jvm\java-17-openjdk-17.0.20.0.8-1.2.el9_8.x86_64` 경로로 수동 추가.
- 최근 빌드된 실행 파일(`weekly-report.jar` + `run.bat`)은 참고용으로 `C:\Users\sanghwaj\Desktop\상화\105_주간보고_260727\dist\`에도 복사해둔 적 있음(프로젝트 자체엔 없음, 재빌드 시 갱신 필요).

## 프론트엔드 (2026-08-02 이식 완료)

목업 디자인을 실제 Thymeleaf + htmx로 이식하는 작업은 **끝났고 앱에 반영되어 있음**. 아래는 그 결정 배경과 현재 구조:

- **레퍼런스**: 사용자가 제공한 Claude 아티팩트 목업(`주간업무보고 대시보드.zip`, 원본은 Desktop에만 있고 프로젝트엔 미포함)의 시각 언어 — 연회색 배경, 흰 카드(얇은 보더·큰 라운드·그림자 거의 없음), 단일 블루 포인트 컬러(`oklch(55% 0.16 260)`), 보더 없이 텍스트처럼 보이다 포커스 시 강조되는 인라인 입력, 점선 "+추가" 고스트 버튼. **다크모드는 의도적으로 배제 — 레퍼런스의 라이트 톤 하나로 고정**하기로 함(사용자 명시적 요청).
- **승인된 목업**: `design/weekly-report-mockup.html` — 순수 HTML/CSS/vanilla JS 자체완결 파일(Claude Artifact로 발행해 검토 완료, 여러 차례 피드백 반영). 실제 데이터 모델(프로젝트/티켓/구분/시간/일수/완료율/개발완료일·테스트예정일·배포예정일/비고/맨위크/이월)을 그대로 반영하되 화면 구조를 **대시보드 랜딩페이지 없이 "작성" / "히스토리" 2탭으로 단순화**함 — 기존 3화면(대시보드/등록/이력) 구조를 대체. 이 파일이 색상 토큰·컴포넌트 스타일·IA의 기준(source of truth)임. **(2026-08-06 갱신: 사용자가 이 2탭 결정을 뒤집어 목업이 대시보드 + 일일 기록을 포함한 3탭 구조로 다시 그려졌고 2026-08-15에 앱에 이식됨 — 현재 IA는 아래 "현재 화면 구조" 절이 기준이다. 색상 토큰·컴포넌트 스타일의 기준이라는 점은 그대로.)**
  - **⚠️ 중요한 정책 변경**: 사용자가 명시적으로 "제출된(지난) 보고서도 항상 수정 가능해야 한다"고 요구해서, `WeeklyReport.status`(draft/submitted)는 잠금 여부가 아니라 단순 상태 표시임 — 제출된 주도 항목 추가/수정/삭제가 전부 가능하고 재제출(다시 내보내기)도 가능. **`docs/project-handoff-summary.md`의 "제출된 보고서는 고정" 규칙은 이 결정으로 폐기됨**(그 문서는 아직 옛 규칙을 담고 있으니 주의). 이식하면서 `EntryService.requireEditable()`과 `HistoryController`의 읽기전용 상세 화면은 제거했다.
  - 항목 입력 행에 컬럼 헤더(일감번호/제목/구분/투입시간/일수/진행률)를 항목이 있을 때만 표시.
  - 일정 상세 패널은 개발완료일/테스트예정일/배포예정일을 한 줄씩(세로 스택) 입력하고 비고를 마지막에 입력하는 순서로 배치.
  - **프로젝트 그룹 입력 구조 (2026-08-02 개정)**: **"프로젝트 = 일감(티켓) 하나"**로 확정 — 한 프로젝트 카드 안에 여러 일감을 두는 구조(및 "+ 일감 추가" 버튼)는 제거함. 이유: 일감 블록에 있던 "제목" 필드가 실제로는 프로젝트명과 항상 같은 값이라 중복이었고, 사용자 워크플로상 프로젝트 하나당 일감도 정확히 하나였음. 카드 헤더에는 **프로젝트명(좌측, 편집 가능) + 일감번호 입력(우측)** 만 표시하고, 그 아래 바로 세부 항목 목록이 옴(중간에 별도 일감 블록 wrapper 없음). 세부 항목은 여전히 "그때그때 추가" 방식 유지 — 구분(분석/설계/개발/테스트)은 각 항목마다 셀렉트로 고르고, **같은 구분을 여러 개 추가할 수 있음**(예: 한 프로젝트에 "로그인 기능"·"2차인증 기능"처럼 개발 항목 2개). **이건 순수 입력 UI 개편이고 데이터 스키마 변경은 아님** — md 내보내기 시에는 손댄 세부 항목마다 여전히 스키마의 flat한 `- {티켓} : {제목} [구분] — {시간}h · {일수}일 · {완료율}%` 한 줄씩으로 풀어서 나감(제목은 세부 항목 제목이 있으면 그것, 없으면 프로젝트명). 즉 백엔드 `ReportItem`(항목 1개 = 줄 1개) 모델은 그대로 두고, Thymeleaf 이식 시 "같은 프로젝트명(=일감)을 가진 항목들을 화면에서만 프로젝트 카드 단위로 묶어 보여주는" 프레젠테이션 레이어만 추가하면 됨.
- **인터랙션 구현 방식: htmx**. React 등 SPA는 명시적으로 기각 — 별도 빌드 파이프라인/새 툴체인이 생기는 비용이 이 정도 규모의 사내 도구엔 과함(폐쇄망·전원 자바 개발자 환경이라 애초에 Thymeleaf를 고른 이유와 직결). 폐쇄망이라 CDN을 못 쓰므로 `static/js/htmx.min.js`(2.0.4)를 저장소에 직접 넣어둠.

### 현재 화면 구조 (2026-08-16 벤토 대시보드 + 할 일 리스트 반영)

- **탭은 대시보드/작성/히스토리 3개이고 첫 진입(`/`)은 대시보드다.** 2026-08-02 이식 때 지웠던 대시보드를 2026-08-06 재기획 결정으로 되살린 것 — 옛 `DashboardController`/`dashboard.html`의 복원이 아니라 새 설계다(읽기전용 상세 화면 `history-detail.html`·`preview.html`은 여전히 없고, `/history/{id}`도 옛 링크 호환용 리다이렉트 그대로). 주차 페이저는 작성 탭에서만 보인다(다른 탭에선 `visibility:hidden` — `display:none`이면 헤더 폭이 튀어 탭 전환 시 레이아웃이 흔들림).
- 라우팅: `/`(대시보드) / `/entry?week=yyyy-MM-dd`(작성) / `/history?view=reports|records&month=yyyy-MM&q=검색어`(히스토리) / `/daily-notes*`(일일 기록 CRUD) / `/todos*`(할 일 CRUD) / `/export/{id}`(md 다운로드) / `/onboarding`.
- **대시보드는 벤토 그리드다 (2026-08-16 확정)** — 서버가 계산해 렌더하는 거의 정적인 화면이고, 위 띠 = *이번 주가 어떤 주인가*(hero + 지표 타일 4개: 이번 주 시간 / 이번 주 맨위크 / 기한 지난 할 일 / 진행중 프로젝트), 아래 띠 = *지금 뭘 적고 뭘 해야 하나*("오늘 한 일" + "앞으로 할 일"), 그 아래 전폭(`.bento-foot`)에 과거 제출본 5건. ⚠️ hero의 총 시간/맨위크는 `WeeklyReport.totalHours`/`totalManWeek`을 **읽지 않고 매번 항목에서 다시 계산**한다 — 그 필드들은 제출 시점에만 채워져 작성중(draft) 주에서 0이기 때문. 과거 보고서 목록 모집단은 **이번 주를 제외한 제출본만**이다.
  - ⚠️ **`dashboard.html`의 타일 나열 순서와 `app.css`의 span 값은 한 세트다** — `.bento`는 `grid-auto-flow`가 dense가 아니라 뒤 타일이 앞 구멍으로 끌려오지 않는다. 순서를 바꾸거나 span 하나만 고치면 마지막 띠에 빈 구멍이 남는다. 현재 값(hero 6×4 / metric 3×2 / daily 8×6 / todo 4×6)은 정확히 10행에 딱 떨어지도록 역산한 것이고, `.t-daily`와 `.t-todo`의 `span 6`은 **항상 같이 움직여야 한다**(두 타일의 상하단이 픽셀 단위로 정렬돼야 벤토로 읽힌다). 작성 탭 `.split`의 680/916/1670과 같은 취급 — 산출 근거는 `_workspace/20260815_095238_dashboard-mockup-exploration/design_summary_v3.md` §2.
  - **"최근 2주 평균 맨위크"·"진행중인 프로젝트" 큰 타일은 대시보드에서 뺐다** — 사용자가 벤토를 고르면서 "참조성 큰 타일은 빼달라"고 결정한 결과다. **정보가 앱에서 사라진 게 아니라 이 화면의 상세 브레이크다운만 사라졌다**: 맨위크는 지표 타일 + 히스토리 탭의 보고서별 맨위크 + 작성 탭 통계에, 진행중 프로젝트는 지표 타일의 **건수 + 평균 진행률**에 남아 있다. 딸려 죽은 `avgManWeek`/`avgWeekCount`/`populateManWeekAverage()`는 2026-08-16에 제거했다(대시보드 진입마다 `populatePastReports()`와 같은 제출본 테이블을 한 번 더 훑고 버리던 비용도 함께 사라짐) — **타일을 되살리려면 이 계산도 같이 되살려야 한다.** 작성 탭에도 `avgManWeek`가 있지만 `EntryController`가 4주 기준으로 따로 채우는 **별개 지표**이니 같은 것으로 보지 말 것.
  - 2026-08-15에 다른 레이아웃 시안 20종(2라운드)을 탐색했지만 사용자가 전부 반려하고 벤토를 유지하기로 확정했다 — 다음에 대시보드 배치를 다시 논하려면 그 탐색 결과(`_workspace/20260815_095238_dashboard-mockup-exploration/`, `_workspace/20260815_145754_dashboard-mockup-round2/`)부터 볼 것. 같은 안을 다시 그리지 않기 위해서다.
- **`Project.active` 집계 규칙 확정 (2026-08-15)**: "진행중인 프로젝트"는 `Project.active == true` **AND 최근 보고된 진행률 < 100%**이며, 진행률은 그 주 세부 항목 완료율의 **평균**(최댓값 아님 — 설계 100% + 개발 20%짜리 프로젝트가 100%로 보이면 안 됨). 대시보드와 작성 탭이 같은 `EntryService.activeProjectsWithProgress()`를 쓰므로 두 화면의 건수가 항상 같다(구현 직후 작성 탭만 단순 active 카운트라 값이 갈렸던 것을 이 규칙으로 통일). 모집단에는 draft도 포함된다 — "최근 보고 주차"는 실제로 마지막에 적은 주를 뜻하므로. 벤토 개편 이후 대시보드는 이 결과의 **건수와 진행률 평균만** 쓴다(프로젝트별 목록은 화면에 없다) — 판정 규칙 자체는 작성 탭과 계속 공유하므로 손대면 두 화면이 같이 움직인다.
- **작성 화면은 좌우 분할** — 왼쪽 "이번 주에 한 일" 일일 기록 패널(sticky), 오른쪽은 기존 `#writeView` 그대로. 이때만 `body.wide`가 붙어 `main`이 1140px → 1670px로 넓어진다("숨기기"를 누르거나 다른 탭으로 가면 즉시 1140px 복귀, 상태는 `localStorage`). ⚠️ `.split{grid-template-columns:minmax(0,680px) minmax(916px,1fr)}`의 **680/916/1670은 임의값이 아니라 계산값**이다(항목 행 최소 폭 752px, 완료+이월 배지 동시 860px + 카드 패딩 56px = 우측 하한 916px). 습관적으로 `minmax(0,1fr)`로 바꾸거나 반올림하면 좁은 화면에서 항목 행이 조용히 두 줄로 깨진다 — 산출 근거와 뷰포트별 실측은 `docs/2026-08-06-daily-notes-design-worklog.md` §6. **항목 행에 새 배지·새 칸을 붙이면 이 하한이 올라가므로 계산을 다시 해야 한다.**
- **히스토리 탭은 서브 세그먼트 2개** — `view=reports`(과거 제출본, 기본) / `view=records`("한 일 기록", 월 단위·최신순). 4번째 탭으로 올리지 않은 이유는 헤더에 "히스토리"와 "기록"이 나란히 서면 한국어로 사실상 동의어라 IA가 깨지기 때문(worklog §5.2). 같은 이유로 헤더 탭 라벨에서 건수 표기를 뺐다.
- **"한 일 기록"의 본문 검색 구현 완료 (2026-08-15)** — `/history?view=records&q=…`. 결정 사항만 적어둔다:
  - **검색 범위는 보고 있는 그 달 안뿐**(전체 기간 아님). 월 페이저가 이 화면의 축이라, 검색이 축을 무시하면 페이저와 목록이 서로 다른 것을 가리킨다. 대신 **월을 넘겨도 검색어는 유지**된다(월 이동 링크가 `q`를 싣는다) — 실제 용례가 "그거 언제 했더라"라 달을 넘겨 가며 훑는 흐름이기 때문. 반대로 `hasPrevMonth`/`hasNextMonth`(페이저 활성 여부)는 **검색과 무관하게 전체 기록 기준**이다 — 검색어가 안 걸리는 달이라고 이동을 막으면 "다른 달엔 있나"를 확인할 방법이 없어진다.
  - 검색 중에는 통계 타일 3칸 대신 `.rec-scope` 한 줄(`2026년 8월에서 "GTPP" — 2건 · 5h · 2일`)로 바뀐다. 건수·시간·일수를 **검색 결과 기준으로 다시 계산**하는데, 그 값이 "이 달 전체"를 읽는 자리에 그대로 앉으면 어느 모집단인지 알 수 없기 때문.
  - **⚠️ 검색 중에는 새 기록을 추가할 수 없다(의도된 설계)** — 추가/삭제 후에도 검색 필터가 유지되므로, 검색어와 안 맞는 기록을 새로 적으면 저장은 됐는데 목록에서 바로 사라진 것처럼 보인다. 그래서 `recordSearching`이 true면 `.quick-add` 폼 자리에 "검색 중에는 새 기록을 추가할 수 없습니다. + 검색 지우기" 안내(`.rec-addlock`)를 대신 놓는다. 폼이 없어진 걸 버그로 잡지 말 것.
- **일일 기록(`DailyNote`)은 주간보고와 완전히 분리된 데이터다** — 다른 엔티티와 FK가 없고, `.md` 내보내기에 나가지 않으며, 기록에 적은 시간은 맨위크·총 투입시간에 **절대 섞이지 않는다**(전부 사용자 확정 사항, 회귀 테스트로 고정). 기록→주간보고 항목 승격/자동등록 장치도 **없다** — v2 설계에서 체크박스 승격을 제안했다가 사용자가 반려했다(기록 여러 건이 항목 하나에 대응하는 다대일 관계라 목적지 자동 판정이 모호). 사람이 왼쪽을 보면서 오른쪽 폼을 직접 채우는 순수 참고 목록이다.
  - 적는 곳은 대시보드 카드 / 작성 패널 / 기록 화면 셋이고 **차이는 범위뿐**(오늘+최근 3일 / 그 주 7일 / 그 달).
  - ⚠️ **정렬 방향이 화면마다 반대인 것은 의도된 설계다** — 작성 패널은 금→목 오름차순(한 주를 처음부터 읽는 자리), 기록 화면은 최신순 내림차순(거슬러 올라가는 자리). 버그로 잡지 말 것.
  - 투입시간은 **선택 입력**이고 빈 값은 0이 아니라 `null`("안 적음"). 합계가 0이면 칩·날짜 헤더 합계를 **아예 렌더하지 않는다**(`0h`를 보여주면 "채워야 할 빈칸"으로 읽혀 선택 입력이라는 성격과 충돌) — 단 기록 화면의 월 통계 타일만 예외로 `0`을 찍는다.
  - 기록은 보고서 존재 여부·상태와 무관하다 — 보고서를 만들지 않은 주에도, 제출된 주에도 그대로 쓰고 고칠 수 있다.
- **할 일 리스트(`TodoItem`)도 주간보고와 완전히 분리된 데이터다 (2026-08-16 구현)** — `DailyNote`와 **같은 급**이다: FK 0개, `.md` 내보내기 미포함, 시간 필드 자체가 없어 맨위크·총 투입시간에 섞일 여지도 없다. 카드 하단에 `개인용 목록입니다. 주간보고(.md)에는 나가지 않습니다.`를 고정 문구로 박아 화면이 스스로 스코프를 못박게 했다. `DailyNote`와의 관계는 **완전 별개** — 할 일을 완료해도 "한 일 기록"으로 넘어가지 않고, 반대 방향 승격도 없다(v2 승격 논쟁의 재현을 피하려는 사용자 확정 사항).
  - `DailyNote`가 **회고형**(날짜별로 한 일을 남김)이라면 이건 **전망형**(앞으로 할 일)이다. 한 테이블에 `type` 컬럼으로 합치는 안은 기각했다 — 모든 쿼리에 분기가 붙고 "완료 체크한 예정이 자동으로 기록이 되나?"라는 승격 질문이 되살아난다. 상세 근거는 `docs/2026-08-15-todo-feature-review.md`.
  - ⚠️ **주(week)에 속하지 않는다** — 대시보드 카드 한 곳에만 있고 라우트 6개 어디에도 `week` 파라미터가 없다. 주간보고 화면 관례를 따라 `week`를 붙이려 하지 말 것.
  - **어휘 분리**: 카드 제목은 **"앞으로 할 일"**(좌측 "오늘 한 일"과 한 글자 차이라 첫 단어부터 갈랐다). **이 카드 안에서 "기록"이라는 단어를 쓰지 않는다** — 회고형 전용 어휘다.
  - **자동 이월·자동 삭제·자동 만료가 전부 없다** — 기한이 지나도 프로그램이 날짜를 옮기지 않고 "기한 지남" 그룹으로 묶어 강조만 한다(날짜를 옮기면 며칠 밀렸는지가 지워짐). 날짜 변경은 사람이 📅로 직접 한다. 완료 항목도 지우지 않고 카드 맨 아래 접힌 섹션으로 내려보낸다(잘못 체크한 것을 되돌릴 수 있어야 하므로).
  - ⚠️ **정렬 방향이 미완료/완료가 서로 반대인 것은 의도된 설계다** — 미완료는 `dueDate` 오름차순 → 우선순위(높음→보통→낮음) → id 오름차순이라 **기한 지난 것이 자연히 맨 위**로 오고, 완료는 `dueDate` 내림차순 → id 내림차순이라 최근 끝낸 것이 위로 온다. 버그로 잡지 말 것.
  - ⚠️ **정렬을 SQL `ORDER BY`로 옮기면 조용히 깨진다** — `priority`가 `@Enumerated(STRING)`이라 DB 정렬은 HIGH→LOW→MID(문자열 순)가 된다. 그래서 `TodoItemService`가 Java 비교자로 정렬한다("레포지토리에서 정렬하면 되지 않나"라며 지우면 회귀 테스트가 깨진다).
  - 우선순위는 3단계(높음/보통/낮음, 기본 보통)이고 **표시와 편집을 pill 버튼 하나가 겸한다**(누르면 순환) — 좁은 카드에 셀렉트를 따로 두지 않기 위해서다. 색은 **높음만** 주황이고 보통/낮음은 배경 없음 — 기본값에서 벗어난 것만 색이 붙어야 목록이 배지밭이 되지 않는다.
  - **미완료 8건(`TODO_VISIBLE`)까지만 펼쳐 보이고 나머지와 완료 목록은 접혀 있는데, 접힘/펼침에 서버 왕복이 없다** — 서버가 **전량을 내려주고** `app.js`가 CSS 클래스로 감춘다. "+N건 더 보기"가 네트워크를 타지 않는 건 의도된 것이다. 펼침 상태는 `localStorage`가 아니라 **메모리 변수**라 새로고침하면 초기값으로 돌아간다(취향 설정이 아니라 그때그때 열람 상태라는 판단 — 작성 탭 `body.wide`와 다른 취급).
  - "기한 지남" 헤더의 건수는 **접힘 여부와 무관하게 전체 기한지남 건수**다. 지표 타일 "기한 지난 할 일"과 같은 숫자여야 하기 때문 — 목업은 잘린 목록 기준이었지만 앱이 의도적으로 다르게 갔다.
- 주차 이동·탭 전환은 평범한 링크(전체 페이지 로드), 나머지 조작만 htmx.
- 보고서는 주차를 넘겨본다고 자동 생성되지 않음 — 없는 주는 빈 상태 화면이고 "작성 시작"을 눌러야 이월 포함 초안이 생긴다. 대시보드의 "신규 작성"도 초안을 즉시 만들지 않고 그 주 작성 화면으로 보내기만 한다(초안 생성 경로는 `POST /entry/start` 하나뿐).

### htmx 배선 규약 (`fragments-entry.html` 주석에도 동일 내용 있음)

- 구조가 바뀌는 동작(추가/삭제/휴가 기간 토글/프로젝트 추가·삭제)은 `#writeView` **전체**를 다시 렌더링해 `hx-swap="outerHTML"`로 갈아끼운다. 카드별 건수·통계·빈 상태가 한 번에 맞아 프래그먼트를 잘게 쪼개는 것보다 안전함.
- 인라인 필드 수정은 `hx-trigger="change" hx-swap="none"` — 화면을 다시 그리면 입력 포커스가 날아가므로 저장만 한다. 그래서 완료율 100% "완료" 배지는 `static/js/app.js`가 클라이언트에서 즉시 붙인다(일정 패널 접기/펴기, 토스트, 모달 닫기도 여기).
- 행 하나가 통째로 `<form>`이고, `week`는 전부 **쿼리스트링**으로 넘긴다(버튼이 폼 안/밖 어디 있든 동일하게 전달됨).
- "md로 내보내기"만 htmx가 아닌 일반 폼 전송 — 응답이 첨부파일이라 브라우저가 직접 받아야 한다.
- **`hx-select`는 딱 한 곳에서만 쓴다(2026-08-15 신규)** — 기록 검색이 `hx-get`으로 부르는 `GET /history`는 프래그먼트 전용 라우트가 아니라 **페이지 전체**를 돌려주므로, 응답에서 `hx-select="#recordsPane"`으로 그 블록만 떼어낸다. **이건 기존 "구조가 바뀌면 블록 통째로 다시 그린다" 규약의 예외가 아니다** — 스왑 단위는 여전히 `hx-target="#recordsPane" hx-swap="outerHTML"`(블록 통째)이고, `hx-select`는 스왑 단위가 아니라 **응답에서 쓸 부분만 고르는 응답측 필터**다. 검색 하나 때문에 새 라우트를 만들지 않기로 한 선택의 대가. `POST /daily-notes…`는 원래 프래그먼트만 주므로 `hx-select` 없이 그대로 갈아끼운다.
  - 검색 입력은 한 글자마다 `#recordsPane`을 통째로 교체하는데도 포커스·캐럿이 유지된다 — htmx가 스왑 전 `activeElement`를 기억했다가 **같은 `id`를 가진 새 요소로 되돌려주기** 때문이다. `id="recSearch"`를 지우면 한 글자 칠 때마다 포커스가 날아간다.
  - **⚠️ 검색어 모델 속성이 두 개인 이유(qa가 실측으로 잡은 버그)**: `recordQuery`는 **정규화된(`strip()`) 값**으로 월 이동 링크·`dayGroup`의 `q` 인자·범위 문구에 쓰고, `recordQueryRaw`는 **정규화 전 원문으로 검색 입력칸 `th:value` 전용**이다. 입력칸에 trim된 값을 돌려주면 `GTPP `까지 치고 300ms 멈추는 순간 스왑이 일어나 **방금 친 공백이 사라지고** 이어 친 글자가 앞말에 붙는다(`GTPP로그인`). "같은 값을 두 번 내려주는 중복"으로 보고 하나로 합치지 말 것. 정규화는 `trim()`이 아니라 `strip()`이어야 한다 — `trim()`은 한글 IME의 전각 공백(U+3000)을 못 떼서 "검색칸은 비어 보이는데 검색 중"인 상태에 갇힌다.
- 일일 기록도 같은 규약을 따른다(2026-08-15) — 추가/삭제는 그 화면의 카드/패널/판을 통째로 다시 그려 `outerHTML`로 갈아끼우고(`view` 파라미터로 세 프래그먼트 중 하나를 고른다), 텍스트·시간 인라인 수정은 `hx-swap="none"`. 그래서 **날짜 헤더 합계와 "오늘 N건 · Xh" 칩은 서버가 갱신해주지 못해 `app.js`가 클라이언트에서 다시 계산한다**(완료율 100% "완료" 배지와 같은 자리). 단 대시보드 하단의 "이번 주 기록 N건"은 화면에 없는 날의 기록까지 포함하는 값이라 DOM만으로 계산할 수 없어 클라이언트 갱신 대상에서 뺐다 — 시간만 인라인 수정한 직후 잠시 옛 값이 남는 것은 의도된 절충이다.
- 할 일 리스트도 같은 규약이다(2026-08-16) — 구조가 바뀌는 5개(추가 `POST /todos`, 완료 토글 `/done`, 우선순위 `/priority`, 기한 `/due`, 삭제 `/delete`)는 전부 `hx-target="#todoCard" hx-swap="outerHTML"`로 카드를 통째 교체하고, 텍스트 인라인 수정(`POST /todos/{id}`)만 `hx-swap="none"`. 이 화면 고유의 계약 몇 가지:
  - **완료·우선순위는 값을 받지 않는다** — 서버가 현재 값을 반전/순환시킨다. 체크 해제된 체크박스는 아예 전송되지 않아 클라이언트가 보낸 값을 믿을 수 없기 때문.
  - 행이 `<form>`이 아니라 `<div>`이고 **컨트롤마다 자기 `hx-post`를 갖는다**(컨트롤별로 목적지가 다름). htmx가 트리거 요소 자신의 `name` 값을 실어 보내므로 `text`·`dueDate`는 그대로 전달된다 — 작성 화면의 "행 하나가 통째로 폼" 규약과 다른 유일한 자리다.
  - **벤토 배치 클래스(`tile t-todo`)가 프래그먼트 루트에 붙어 있어야 한다** — `outerHTML`로 갈아끼우므로 `dashboard.html` 쪽에서 감싸면 첫 스왑에 사라진다.
  - ⚠️ **htmx는 `class`를 `attributesToSettle` 대상으로 삼는다(실측으로 잡은 버그)** — 스왑 직후 옛 class를 잠시 유지했다가 settle(기본 20ms 뒤)에 **서버가 준 값으로 되돌린다**. 그래서 `htmx:afterSwap`에서 붙인 `.expanded`는 조용히 지워졌고, 펼쳐둔 목록이 할 일 하나 추가할 때마다 다시 접히는데 버튼 라벨은 "접기"인 상태가 됐다(라벨은 textContent라 살아남는다). 클래스 복원은 반드시 `htmx:afterSettle`에서 한다. 반대로 `data-*`는 settle 대상이 아니라 스왑 직후 이미 새 값이다 — 지표 타일 동기화가 `afterSwap`에서 도는 이유.
  - ⚠️ **감춰진 textarea는 `scrollHeight`가 0이라 자동 높이가 `0px`로 굳는다** — 접힌 행·완료 행이 그 상태로 펼쳐지면 글자가 안 보였다. `autoGrow`는 0이면 인라인 높이를 지우고, 펼치는 쪽은 `classList.toggle` **뒤에** 다시 잰다(순서가 바뀌면 또 0을 읽는다).

### 이식하며 실제로 밟은 함정

- **Thymeleaf는 `th:replace`를 `th:each`/`th:if`보다 먼저 처리한다.** 같은 태그에 같이 쓰면 반복/조건이 적용되기 전에 프래그먼트가 불려서 인자가 null로 들어가고 `EL1007E: Property or field 'id' cannot be found on null`이 난다. 반드시 바깥 `<th:block th:each>` + 안쪽 `<div th:replace>`로 나눌 것.
- `th:text`를 바깥 div에 걸면 안쪽 자식(예: 주차 라벨의 `.range` span)까지 지워진다.
- BigDecimal은 DB에서 `8.00`으로 돌아와 좁은 입력칸에서 잘린다 — 표기용으로 `ReportItem.hoursDisplay()` / `WeeklyReport.totalHoursDisplay()`가 뒷자리 0을 떼준다.
- `./gradlew bootRun`은 `build/resources/main`을 읽으므로, 실행 중에 `src/main/resources`의 템플릿을 고쳐도 반영되지 않는다(= 재시작 필요). `thymeleaf.cache=false`만 믿고 헤매지 말 것.

## md 내보내기 포맷 (2026-08-05 확정)

- **⚠️ 중요한 정책 변경**: 사용자가 "json은 배제하고 최대한 마크다운을 틀에 맞춰서 파싱하는 형태로 가야할 거 같다"고 결정해서, `.md` 파일 끝에 붙던 **`<!--DATA...DATA-->` JSON 블록을 완전히 제거**했다(`MdExportService`에서 직렬화 로직 삭제). 이제 사람이 읽는 마크다운 본문과 프로그램이 파싱하는 대상이 **같은 텍스트 하나**다 — 본문 포맷을 손대면 곧 파트장/팀장 프로그램(미개발, 범위 밖)과의 계약이 깨지므로, `MdExportService`를 고칠 때 `docs/weekly-report-md-schema.md`를 반드시 함께 갱신할 것. **v1 스키마의 JSON 블록 정의는 이 결정으로 폐기됨**(스펙 문서는 v2로 재작성 완료).
- 같이 확정된 본문 문법 변경 두 가지:
  - project/dev 항목 줄에 **일수를 항상 표시** — `- {티켓 : }{제목}{ [구분]} — {시간}h · {일수}일 · {완료율}%{ (이월)}`. 시간이 없으면 `{시간}h · `만 통째로 빠지고, 일수는 `daysOrDefault()`가 미입력을 `1`로 채우므로 절대 생략되지 않는다(JSON이 사라진 이상 일수를 본문에 안 적으면 복원할 방법이 없어서).
  - 헤더 2행 날짜에 **연도 포함** — `파트원 · 2026.07.31 (금) ~ 2026.08.06 (목)` (v1은 `07.31 (금)`처럼 연도가 없어 파일명의 "8월 1주"에만 의존해야 했음).
- `[구분]` 대괄호에는 짧은 라벨(`설계`/`개발`/`테스트`)이 나간다 — `Phase.ANALYSIS_DESIGN`의 내부 라벨은 `분석/설계`인데 본문엔 `[설계]`로 표기되므로, 나중에 파서를 만들 때 되돌리는 매핑이 필요하다.
- **v1(JSON 블록 포함) 파일은 하위호환 지원 안 함** — 파트장 프로그램이 아직 미개발이라 실사용 v1 파일이 사실상 없어, 파서 복잡도를 감수할 이유가 없다고 판단. 향후 파서는 v2(마크다운 전용) 형식만 가정한다.

## 다음에 할 일 (2026-08-16 기준 미결)

- **`docs/project-handoff-summary.md` 자체는 아직 옛 "제출 후 고정" 규칙을 담고 있어 실제 동작과 어긋남** — 문서를 현재 정책에 맞게 고칠지 결정 필요 (현재는 CLAUDE.md의 각주로만 "폐기됨"을 표시해둔 상태). `docs/2026-08-06-role-roadmap.md` §3·§5-7이 지적한 "역할별 별도 프로그램" 서술까지 포함해 **원본의 신뢰도 표시 방식 자체를 한 번 정할 문제**다. 손댈 곳: `docs/project-handoff-summary.md`(원본 수정 여부는 사용자 결정 사항 — 임의로 재작성하지 말 것).
- **파트원/파트장/팀장 3역할 확장 로드맵 정리 완료(2026-08-06), 파트장/팀장 구현 미착수** — 새 사용자/역할 체계가 필요한 별도 범위. 로드맵 §2 "파트원 개인 일정관리"는 **회고형(2026-08-15 일일 기록)·전망형(2026-08-16 할 일 리스트) 양쪽 다 해소됐다** — 다만 로드맵이 전망형에 상정했던 "이월"은 사용자 결정으로 스코프에서 빠졌고(자동 이월 없음), 기존 일정 필드(`ReportItem`의 개발완료일/테스트예정일/배포예정일, 휴가 그룹 날짜)와의 관계는 **여전히 정하지 않은 채 남아 있다** — `TodoItem`은 그 필드들과 FK 없이 완전히 분리해 만들었으므로, 나중에 둘을 엮자는 요구가 나오면 그때 새로 설계해야 한다. 손댈 곳: `docs/2026-08-06-role-roadmap.md`(§3 이후 파트장/팀장 범위).

## 알아두면 좋은 것

- **개발 그룹의 티켓번호도 필수로 유지하기로 결정(2026-08-15)** — project/dev 그룹 모두 티켓번호 없으면 제출이 막히는 현재 검증(`EntryService`)을 그대로 둔다. 승인된 목업엔 티켓 없는 개발 항목 예시가 있었지만, 선택(optional)으로 풀지 않기로 확정했다 — 코드 변경 없음.
- `Group` 등 JPA 연관관계가 있는 엔티티(`Project` 등)는 `equals()`/`hashCode()`를 id 기준으로 반드시 오버라이드해야 함 — 다른 쿼리(다른 영속성 컨텍스트)에서 로딩된 동일 row가 기본 identity equals로 인해 다른 객체로 취급되는 버그를 실제로 겪음. proxy 필드 직접 접근(`other.id`) 대신 반드시 getter(`other.getId()`) 사용 — 그렇지 않으면 Hibernate 지연 로딩 프록시에서 항상 null 반환함.
- SQL 예약어(`group` 등)를 컬럼명으로 쓰면 H2에서 구문 오류 — `@Column(name = "group_type")`처럼 명시적으로 피해야 함.
